<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Carnet de Montagne</a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_dashboard') }}">Tableau de bord</a>
                    </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Mes carnets
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_user_note_book',{id:1}) }}">Grandes-Voies</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_user_note_book',{id:2}) }}">Grandes-Voies Trad</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_user_note_book',{id:3}) }}">Alpinisme-rocheux</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_user_note_book',{id:4}) }}">Alpinisme-mixte</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_user_note_book',{id:5}) }}">Randonnée</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_user_note_book',{id:6}) }}">Randonnée-Alpine</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item text-primary" href="{{ path('app_notebook_new',{id:1}) }}">Ajouter une sortie</a>
                        </li>
                    </ul>
                </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Profil</a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_user_edit',{id:app.user.id}) }}">Modifer mon profil</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_user_edit_password',{id:app.user.id}) }}">Modifer mon mot de passe</a></li>
                        </ul>
                    </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Deconnection</a>
                    </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_auth') }}">Connection</a>
                </li>{% endif %}

            </ul>
            <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="Rechercher" aria-label="Search">
                <button class="btn btn-secondary" type="submit">Rechercher</button>
            </form>
            <a class="btn btn-info m-lg-2" href="{{ path('app_contact') }}">Contact</a>
        </div>
    </div>
</nav>